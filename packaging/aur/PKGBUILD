# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-miden-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-miden-recorder (CodeTracer recorder for Miden VM programs (MASM and Rust-via-midenc)).

pkgname=codetracer-miden-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for Miden VM programs (MASM and Rust-via-midenc)"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-miden-recorder"
license=('Apache-2.0')
depends=('glibc' 'zstd')
makedepends=('rust' 'cargo')
provides=("codetracer-miden-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-miden-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-miden-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-miden-recorder
}

check() {
  cd "$srcdir/codetracer-miden-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-miden-recorder || true
}

package() {
  cd "$srcdir/codetracer-miden-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-miden-recorder" \
    "$pkgdir/usr/bin/codetracer-miden-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
